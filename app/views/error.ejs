<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - EDU LMS</title>
    <%- include('./partials/header') %>
    <style>
        .error-container {
            text-align: center;
            padding: 100px 20px;
            max-width: 600px;
            margin: 0 auto;
        }
        .error-icon {
            font-size: 4rem;
            margin-bottom: 2rem;
        }
        .error-title {
            font-size: 2rem;
            margin-bottom: 1rem;
            color: #333;
        }
        .error-message {
            font-size: 1.125rem;
            color: #666;
            margin-bottom: 2rem;
            line-height: 1.6;
        }
        .btn-home {
            background: #667eea;
            color: white;
            padding: 12px 30px;
            text-decoration: none;
            border-radius: 6px;
            display: inline-block;
        }
    </style>
</head>
<body>
    <%- include('./partials/navigation') %>
    
    <div class="error-container">
        <div class="error-icon">⚠️</div>
        <h1 class="error-title"><%= title || 'Something went wrong' %></h1>
        <p class="error-message">
            <%= message || 'We encountered an unexpected error. Please try again later.' %>
        </p>
        <% if (error && process.env.NODE_ENV === 'development') { %>
            <details style="text-align: left; margin: 2rem 0;">
                <summary>Technical Details</summary>
                <pre style="background: #f5f5f5; padding: 1rem; border-radius: 6px; overflow-x: auto;"><%= error.stack %></pre>
            </details>
        <% } %>
        <a href="/" class="btn-home">Go Back Home</a>
    </div>

    <%- include('./partials/footer') %>
</body>
</html>