<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= assignment.title %> - Teacher Portal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #3b82f6;
            --primary-dark: #1e40af;
            --secondary: #64748b;
            --success: #10b981;
            --warning: #f59e0b;
            --danger: #ef4444;
            --light: #f8fafc;
            --dark: #0f172a;
            --sidebar-bg: #1e293b;
            --card-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            --hover-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background-color: #f1f5f9;
            color: #334155;
            line-height: 1.6;
        }

        .view-assignment-page {
            min-height: 100vh;
            display: flex;
        }

        /* Sidebar Styles */
        .sidebar {
            background: linear-gradient(180deg, var(--sidebar-bg) 0%, var(--dark) 100%);
            width: 280px;
            min-height: 100vh;
            padding: 0;
            box-shadow: 2px 0 10px rgba(0,0,0,0.1);
            position: fixed;
            z-index: 100;
            overflow-y: auto;
        }

        /* Main Content */
        .main-content {
            flex: 1;
            margin-left: 280px;
            padding: 0;
            background: #f8f9fa;
        }

        .container-fluid {
            padding: 2rem;
        }

        /* Header */
        .page-header h1 {
            font-weight: 700;
            color: var(--dark);
        }

        .page-header p {
            color: var(--secondary);
            font-size: 1.1rem;
        }

        /* Card Styles */
        .card {
            border-radius: 12px;
            border: none;
            box-shadow: var(--card-shadow);
            overflow: hidden;
        }

        .card .card-header {
            background: white;
            border-bottom: 1px solid #e2e8f0;
            padding: 1.5rem;
        }

        .card .card-body {
            padding: 1.5rem;
        }

        /* Stats Cards */
        .stat-card {
            text-align: center;
            padding: 1.5rem;
        }

        .stat-card .number {
            font-size: 2rem;
            font-weight: 700;
            color: var(--primary);
        }

        .stat-card .label {
            color: var(--secondary);
            font-size: 0.875rem;
            margin-top: 0.5rem;
        }

        /* Submission List */
        .submission-item {
            border: 1px solid #e2e8f0;
            border-radius: 8px;
            padding: 1rem;
            margin-bottom: 0.75rem;
            transition: all 0.3s ease;
        }

        .submission-item:hover {
            border-color: var(--primary);
            box-shadow: var(--card-shadow);
        }

        .submission-item.graded {
            background: #f0fdf4;
            border-color: #bbf7d0;
        }

        .submission-item.pending {
            background: #fffbeb;
            border-color: #fed7aa;
        }

        /* Button Styles */
        .btn {
            border-radius: 8px;
            font-weight: 500;
            padding: 0.75rem 1.5rem;
            transition: all 0.3s ease;
        }

        .btn-primary {
            background: var(--primary);
            border: none;
        }

        .btn-primary:hover {
            background: var(--primary-dark);
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
        }

        .btn-outline-secondary {
            color: var(--secondary);
            border-color: var(--secondary);
        }

        .btn-outline-secondary:hover {
            background: var(--secondary);
            color: white;
            transform: translateY(-2px);
        }

        /* Mobile Menu Button */
        .mobile-menu-btn {
            display: none;
            position: fixed;
            top: 1rem;
            left: 1rem;
            z-index: 101;
            background: var(--primary);
            color: white;
            border: none;
            border-radius: 8px;
            width: 40px;
            height: 40px;
            align-items: center;
            justify-content: center;
            box-shadow: var(--card-shadow);
        }

        /* Responsive Design */
        @media (max-width: 992px) {
            .sidebar {
                transform: translateX(-100%);
                transition: transform 0.3s ease;
            }
            
            .sidebar.active {
                transform: translateX(0);
            }
            
            .main-content {
                margin-left: 0;
            }
            
            .mobile-menu-btn {
                display: flex;
            }
        }

        @media (max-width: 768px) {
            .container-fluid {
                padding: 1.5rem;
            }

            .stat-card {
                padding: 1rem;
            }

            .stat-card .number {
                font-size: 1.5rem;
            }
        }

        /* Badge Styles */
        .badge {
            font-weight: 500;
            padding: 0.5rem 0.75rem;
            border-radius: 6px;
        }

        /* Progress Bar */
        .progress {
            height: 8px;
            border-radius: 4px;
        }

        .progress-bar {
            border-radius: 4px;
        }
    </style>
</head>
<body>
    <div class="view-assignment-page">
        <!-- Mobile Menu Button -->
        <button class="mobile-menu-btn" id="mobileMenuButton">
            <i class="fas fa-bars"></i>
        </button>

        <!-- Sidebar -->
        <div class="sidebar">
            <%- include('../partials/sidebar', { menu: menu, activePage: 'assignments', user: user }) %>
        </div>

        <!-- Main Content -->
        <main class="main-content">
            <div class="container-fluid py-4">
                <!-- Header -->
                <div class="d-flex justify-content-between align-items-center mb-4 page-header">
                    <div>
                        <h1 class="h2 mb-1"><%= assignment.title %></h1>
                        <p class="text-muted mb-0">
                            <span class="badge bg-light text-dark me-2"><%= assignment.course_name %></span>
                            <% if (assignment.due_date) { %>
                                Due: <%= new Date(assignment.due_date).toLocaleString() %>
                            <% } else { %>
                                No due date
                            <% } %>
                        </p>
                    </div>
                    <div class="d-flex gap-2">
                        <a href="/teacher/assignments/<%= assignment.id %>/edit" class="btn btn-outline-primary">
                            <i class="fas fa-edit me-2"></i>Edit
                        </a>
                        <a href="/teacher/assignments" class="btn btn-secondary">
                            <i class="fas fa-arrow-left me-2"></i>Back
                        </a>
                    </div>
                </div>

                <div class="row">
                    <!-- Assignment Details -->
                    <div class="col-lg-8 mb-4">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="card-title mb-0">
                                    <i class="fas fa-info-circle me-2 text-primary"></i>
                                    Assignment Details
                                </h5>
                            </div>
                            <div class="card-body">
                                <% if (assignment.description) { %>
                                    <div class="mb-4">
                                        <h6 class="text-muted mb-2">Description</h6>
                                        <p class="mb-0" style="white-space: pre-wrap;"><%= assignment.description %></p>
                                    </div>
                                <% } %>
                                
                                <div class="row">
                                    <div class="col-md-6">
                                        <div class="mb-3">
                                            <h6 class="text-muted mb-2">Course</h6>
                                            <p class="mb-0"><%= assignment.course_name %></p>
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="mb-3">
                                            <h6 class="text-muted mb-2">Created</h6>
                                            <p class="mb-0"><%= new Date(assignment.created_at).toLocaleString() %></p>
                                        </div>
                                    </div>
                                </div>
                                
                                <% if (assignment.due_date) { %>
                                    <div class="alert alert-<%= new Date(assignment.due_date) < new Date() ? 'danger' : 'warning' %>">
                                        <div class="d-flex align-items-center">
                                            <i class="fas fa-clock me-2 fa-lg"></i>
                                            <div>
                                                <strong>
                                                    <%= new Date(assignment.due_date) < new Date() ? 'Assignment is overdue' : 'Due date approaching' %>
                                                </strong>
                                                <br>
                                                Due: <%= new Date(assignment.due_date).toLocaleString() %>
                                            </div>
                                        </div>
                                    </div>
                                <% } %>
                            </div>
                        </div>

                        <!-- Submissions List -->
                        <div class="card mt-4">
                            <div class="card-header">
                                <h5 class="card-title mb-0">
                                    <i class="fas fa-file-alt me-2 text-info"></i>
                                    Student Submissions (<%= submissions.length %>)
                                </h5>
                            </div>
                            <div class="card-body">
                                <% if (submissions.length > 0) { %>
                                    <div class="submission-list">
                                        <% submissions.forEach(submission => { %>
                                            <div class="submission-item <%= submission.grade !== null ? 'graded' : 'pending' %>">
                                                <div class="d-flex justify-content-between align-items-start">
                                                    <div class="flex-grow-1">
                                                        <h6 class="mb-1"><%= submission.student_name %></h6>
                                                        <p class="text-muted mb-1">
                                                            Submitted: <%= new Date(submission.submitted_at).toLocaleString() %>
                                                        </p>
                                                        <% if (submission.content) { %>
                                                            <p class="mb-2"><%= submission.content.substring(0, 100) %>...</p>
                                                        <% } %>
                                                    </div>
                                                    <div class="text-end">
                                                        <% if (submission.grade !== null) { %>
                                                            <span class="badge bg-success mb-2"><%= submission.grade %>%</span>
                                                            <br>
                                                            <small class="text-muted">Graded</small>
                                                        <% } else { %>
                                                            <span class="badge bg-warning mb-2">Not graded</span>
                                                            <br>
                                                            <small class="text-muted">Pending</small>
                                                        <% } %>
                                                    </div>
                                                </div>
                                                <div class="mt-2">
                                                    <a href="/teacher/submissions/<%= submission.id %>" class="btn btn-sm btn-outline-primary">
                                                        <i class="fas fa-eye me-1"></i>View & Grade
                                                    </a>
                                                </div>
                                            </div>
                                        <% }); %>
                                    </div>
                                <% } else { %>
                                    <div class="text-center py-4">
                                        <i class="fas fa-inbox fa-3x text-muted mb-3"></i>
                                        <h4 class="text-muted">No Submissions Yet</h4>
                                        <p class="text-muted">Students haven't submitted this assignment yet.</p>
                                    </div>
                                <% } %>
                            </div>
                        </div>
                    </div>

                    <!-- Submission Stats -->
                    <div class="col-lg-4 mb-4">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="card-title mb-0">
                                    <i class="fas fa-chart-bar me-2 text-success"></i>
                                    Submission Stats
                                </h5>
                            </div>
                            <div class="card-body">
                                <div class="text-center mb-4">
                                    <div class="position-relative d-inline-block">
                                        <canvas id="submissionChart" width="120" height="120"></canvas>
                                        <div class="position-absolute top-50 start-50 translate-middle">
                                            <h4 class="mb-0"><%= Math.round((submissions.length / 25) * 100) %>%</h4>
                                            <small class="text-muted">Submitted</small>
                                        </div>
                                    </div>
                                </div>
                                
                                <div class="row text-center mb-3">
                                    <div class="col-6">
                                        <div class="stat-card">
                                            <div class="number"><%= submissions.length %></div>
                                            <div class="label">Submitted</div>
                                        </div>
                                    </div>
                                    <div class="col-6">
                                        <div class="stat-card">
                                            <div class="number"><%= 25 - submissions.length %></div>
                                            <div class="label">Pending</div>
                                        </div>
                                    </div>
                                </div>

                                <div class="mb-3">
                                    <div class="d-flex justify-content-between mb-1">
                                        <small>Submission Progress</small>
                                        <small><%= Math.round((submissions.length / 25) * 100) %>%</small>
                                    </div>
                                    <div class="progress">
                                        <div class="progress-bar bg-success" 
                                             style="width: <%= (submissions.length / 25) * 100 %>%">
                                        </div>
                                    </div>
                                </div>

                                <div class="list-group list-group-flush">
                                    <div class="list-group-item d-flex justify-content-between align-items-center border-0 px-0">
                                        <span>Total Students</span>
                                        <span class="badge bg-secondary">25</span>
                                    </div>
                                    <div class="list-group-item d-flex justify-content-between align-items-center border-0 px-0">
                                        <span>Submissions</span>
                                        <span class="badge bg-primary"><%= submissions.length %></span>
                                    </div>
                                    <div class="list-group-item d-flex justify-content-between align-items-center border-0 px-0">
                                        <span>Graded</span>
                                        <span class="badge bg-success"><%= submissions.filter(s => s.grade !== null).length %></span>
                                    </div>
                                    <div class="list-group-item d-flex justify-content-between align-items-center border-0 px-0">
                                        <span>Pending Grade</span>
                                        <span class="badge bg-warning"><%= submissions.filter(s => s.grade === null).length %></span>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Quick Actions -->
                        <div class="card mt-4">
                            <div class="card-header">
                                <h5 class="card-title mb-0">
                                    <i class="fas fa-bolt me-2 text-warning"></i>
                                    Quick Actions
                                </h5>
                            </div>
                            <div class="card-body">
                                <div class="d-grid gap-2">
                                    <a href="/teacher/assignments/<%= assignment.id %>/edit" class="btn btn-outline-primary">
                                        <i class="fas fa-edit me-2"></i>Edit Assignment
                                    </a>
                                    <a href="/teacher/submissions?assignment=<%= assignment.id %>" class="btn btn-primary">
                                        <i class="fas fa-file-alt me-2"></i>Grade Submissions
                                    </a>
                                    <a href="/teacher/assignments/create" class="btn btn-outline-info">
                                        <i class="fas fa-copy me-2"></i>Duplicate Assignment
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Mobile menu toggle
            const mobileMenuButton = document.getElementById('mobileMenuButton');
            const sidebar = document.querySelector('.sidebar');
            
            if (mobileMenuButton && sidebar) {
                mobileMenuButton.addEventListener('click', function() {
                    sidebar.classList.toggle('active');
                });
            }

            // Submission chart
            const ctx = document.getElementById('submissionChart')?.getContext('2d');
            if (ctx) {
                const submitted = <%= submissions.length %>;
                const total = 25;
                const pending = total - submitted;
                
                new Chart(ctx, {
                    type: 'doughnut',
                    data: {
                        labels: ['Submitted', 'Pending'],
                        datasets: [{
                            data: [submitted, pending],
                            backgroundColor: ['#10b981', '#e2e8f0'],
                            borderWidth: 0
                        }]
                    },
                    options: {
                        cutout: '70%',
                        plugins: {
                            legend: {
                                display: false
                            }
                        }
                    }
                });
            }
        });
    </script>
</body>
</html>